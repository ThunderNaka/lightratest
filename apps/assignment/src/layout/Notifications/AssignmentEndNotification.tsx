import React from "react";

/* import { type Notification } from "~/api/notifications"; */

/* interface AssignmentEndNotificationProps {
  notification: Notification;
} */
export const AssignmentEndNotification =
  (/* { notification }: AssignmentEndNotificationProps */) => {
    return (
      <>
        {/*  <div className="mr-1 w-2">
        <div
          className={tw(
            "relative mt-6 inline-block h-0.5 scale-50 rounded-full border-white bg-complementary-blue-500 p-2 align-middle text-xs",
            !notification.readAt && "hidden",
          )}
        />
      </div>
      <div className="ml-1 mr-2 h-16 w-16 pt-2">
        <img
          className="flex-none scale-150 rounded-full bg-gray-50"
          src={notification.imageUrl}
          alt=""
        />
        <div
          className={tw(
            "relative -right-8 bottom-2 w-5 rounded-full border-white p-0.5",
            notification.hoursLeft < 8 && "bg-complementary-red-500",
            notification.hoursLeft >= 8 &&
              notification.hoursLeft < 48 &&
              "bg-complementary-orange-500",
            notification.hoursLeft >= 48 && "bg-complementary-green-600",
          )}
        >
          <ClockIcon className="h-4 w-4 text-white" aria-hidden="true" />
        </div>
      </div>
      <div className="min-w-0 text-left">
        <p className="text-sm text-gray-900">
          <strong className="font-semibold">{notification.name}</strong> has{" "}
          {notification.hoursLeft} hours left assigned to{" "}
          <strong className="font-semibold text-pink-600">
            {notification.project}
          </strong>
          .
        </p>
        <p className="mt-2 flex items-center text-xs text-complementary-blue-500">
          Administrate hours
          <ArrowTopRightOnSquareIcon
            className="mb-0.5 ml-2 h-4 w-4"
            aria-hidden="true"
          />
        </p>
      </div> */}
      </>
    );
  };
